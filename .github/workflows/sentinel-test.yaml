name: Sentinel tests

on:
  pull_request:
    paths: ['sentinel/**']

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: hashicorp/setup-sentinel@v1
      - run: cd sentinel && sentinel test -verbose ./policies/
