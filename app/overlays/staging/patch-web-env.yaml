apiVersion: apps/v1
kind: Deployment
metadata:
  name: web
  namespace: consul
spec:
  template:
    metadata:
      labels:
        tags.datadoghq.com/env: "staging"
    spec:
      containers:
        - name: web
          env:
            - name: DD_ENV
              value: "staging"
