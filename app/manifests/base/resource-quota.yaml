apiVersion: v1
kind: ResourceQuota
metadata:
  name: consul-quota
  namespace: consul
spec:
  hard:
    requests.cpu: "4"
    requests.memory: 4Gi
    limits.cpu: "8"
    limits.memory: 8Gi
    pods: "50"
    services: "20"
